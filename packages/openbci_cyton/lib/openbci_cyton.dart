/// The OpenBCI Cyton (and Cyton + Daisy) over its USB dongle, in pure
/// Dart: board commands, channel settings, and the streamed samples in µV
/// (decoded as BrainFlow does).
library;

export 'src/board.dart';
export 'src/protocol.dart';
