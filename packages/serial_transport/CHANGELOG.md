## 0.1.0

- Initial version, from libhyperscanner 0.4.1: native serial ports
  (Linux, macOS, Windows) and WebSerial. Ports report their USB product
  string instead of whether they are a Hyperscanner; `requestPort` takes a
  `where` preference.
