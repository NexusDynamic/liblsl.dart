/// A genuinely peer-to-peer transport for `peer_coordinator`.
///
/// Re-exports the coordination framework so an application needs one import for
/// the common case; the transport itself is in
/// `package:webrtc_coordinator/transports/webrtc.dart`.
library;

export 'package:peer_coordinator/peer_coordinator.dart';
